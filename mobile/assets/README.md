# Mobile assets placeholder

Add the production logo file at:
- `mobile/assets/logo.png`

This path is already referenced by:
- `mobile/theme/tokens.json` (`assets.logo`)
- `mobile/theme/bb-theme.md`
- platform plans (`mobile/platforms/*.md`)

So once `logo.png` is added, app builds can consume it without further doc changes.
